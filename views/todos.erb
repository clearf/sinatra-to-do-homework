<h3>My (Ever Growing) To Do List</h3>
<ul>
  <% @todos.each do |single_todo| %>
    <li>
      <a href=""><%= single_todo['task'].capitalize %></a>
        (do by: <%= single_todo['due_date'] %> | status:
        <% if single_todo['due_date'] == true %>
          <%= "done!" %>
        <% else %>
          <%= "not done" %>)
        <% end %>
    </li>
  <% end %>
</ul>